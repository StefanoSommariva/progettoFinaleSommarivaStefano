import { IndirizzoOperativo } from './indirizzo-operativo';

describe('IndirizzoOperativo', () => {
  it('should create an instance', () => {
    expect(new IndirizzoOperativo()).toBeTruthy();
  });
});
